---
description: "[legado] Usar regra global token-efficiency.mdc"
alwaysApply: false
---

Context-mode: `~/.cursor/rules/token-efficiency.mdc`. Comandos repo (`npm run build`, `git diff`, etc.) → `ctx_*` MCP conforme regra global.
